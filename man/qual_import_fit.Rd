% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{qual_import_fit}
\alias{qual_import_fit}
\title{Import a live nlmixr2 fit object as a thread-tagged qualification reference}
\usage{
qual_import_fit(
  fit,
  model_name,
  threads,
  method = NULL,
  control = NULL,
  description = "",
  stochastic = NULL,
  category = "",
  seed = NULL,
  initial_estimates = NULL
)
}
\arguments{
\item{fit}{A fitted nlmixr2 object (e.g. from \code{\link[nlmixr2est:nlmixr2]{nlmixr2est::nlmixr2()}}).}

\item{model_name}{Human name for the model (used in the reference id).}

\item{threads}{Integer thread count the fit was produced at (REQUIRED).}

\item{method}{Optional est method override; default read from \code{fit$est}.}

\item{control}{Optional named list of fit-control settings for re-fits.}

\item{description}{Optional model description string.}

\item{stochastic}{Optional logical; default inferred from the method.}

\item{category}{Optional method-family label for tolerance reporting.}

\item{seed}{Optional integer random seed for stochastic methods; see
\code{\link[=qual_import_bundle]{qual_import_bundle()}}'s \code{seed} argument (same resolution rules, reading
off \code{fit$control} when not supplied explicitly).}

\item{initial_estimates}{Starting values for re-fitting this reference;
see \code{\link[=qual_import_bundle]{qual_import_bundle()}}'s \code{initial_estimates} argument (same
defaulting to \code{fit$iniDf0}).}
}
\value{
A reference list (see \code{\link[=qual_reference_write]{qual_reference_write()}}).
}
\description{
The in-memory alternative to \code{\link[=qual_import_bundle]{qual_import_bundle()}}: builds a reference
directly from a fit object already in the R session, with no
\code{nlmixr2save::saveFit()}/\code{loadFit()} round-trip. The model source is
generated with \code{\link[nlmixr2save:saveFitItem]{nlmixr2save::saveFitItem()}} (the same code \code{saveFit()}
itself uses to write a bundle's \code{-ui.R}), and the input data and
estimation method are read directly off the fit (\code{fit$origData},
\code{fit$est}). A fit does not record its rxode2 thread count, so \code{threads}
must be supplied by the caller (the count the fit was produced at).
}
