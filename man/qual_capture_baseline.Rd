% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capture.R
\name{qual_capture_baseline}
\alias{qual_capture_baseline}
\title{Recut the canonical baseline (deliberate; single-threaded)}
\usage{
qual_capture_baseline(
  registry,
  dir = .qual_pkg_file("baseline"),
  fitter = qual_fit_entry,
  methods = qual_methods()
)
}
\arguments{
\item{registry}{The model registry (from \code{\link[=qual_registry]{qual_registry()}}).}

\item{dir}{Output baseline directory.}

\item{fitter}{Function(entry, category, threads) -> fingerprint.}

\item{methods}{The method table (from \code{\link[=qual_methods]{qual_methods()}}); maps method->category.}
}
\value{
Invisibly, the baseline directory.
}
\description{
Uses the EXISTING frozen datasets; never regenerates data. Writes one
fingerprint file per model to \code{dir}, plus a provenance record.
}
