% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{qual_import_bundle}
\alias{qual_import_bundle}
\title{Import an nlmixr2save bundle as a thread-tagged qualification reference}
\usage{
qual_import_bundle(
  zip,
  model_name,
  threads,
  method = NULL,
  control = NULL,
  description = "",
  stochastic = NULL,
  category = "",
  seed = NULL,
  initial_estimates = NULL
)
}
\arguments{
\item{zip}{Path to a \code{.zip} from \code{\link[nlmixr2save:saveFit]{nlmixr2save::saveFit()}}.}

\item{model_name}{Human name for the model (used in the reference id).}

\item{threads}{Integer thread count the bundle was fit at (REQUIRED).}

\item{method}{Optional est method override; default read from the bundle.}

\item{control}{Optional named list of fit-control settings for re-fits.}

\item{description}{Optional model description string.}

\item{stochastic}{Optional logical; default inferred from the method.}

\item{category}{Optional method-family label for tolerance reporting.}

\item{seed}{Optional integer random seed for stochastic methods (\code{saem},
\code{fsaem}, \code{imp}, \code{impmap}, \code{qrpem}). \code{NULL} (default) uses the seed
already present in the bundle's own fit control, if any (e.g. the
bundle was fit with \code{saemControl(seed = 99)}); an explicit value
overrides that. \code{\link[=qual_reference_refit]{qual_reference_refit()}} passes the stored seed into the
method-appropriate control constructor, making a same-seed re-fit of a
stochastic method exactly reproducible rather than merely
tolerance-compared.}

\item{initial_estimates}{Starting values for re-fitting this reference.
\code{NULL} (default) uses the bundle's own pre-fit priors (\code{fit$iniDf0},
preserved by \code{nlmixr2save::saveFit()}) -- NOT the fitted/post-fit values
baked into the reconstructed model source, so a local re-fit reproduces
the original cold-start optimizer trajectory instead of warm-starting
from the answer. Otherwise a named numeric vector/list overriding
specific parameter names (unnamed ones keep the bundle's iniDf0 value),
or a compiled rxode2/nlmixr2 model object (or an unevaluated model
function) whose own \verb{$iniDf} starting values are used instead.}
}
\value{
A reference list (see \code{\link[=qual_reference_write]{qual_reference_write()}}).
}
\description{
Reconstructs the fit with \code{\link[nlmixr2save:loadFit]{nlmixr2save::loadFit()}}, extracts a fingerprint,
and embeds the model source and input data so the reference is self-contained.
A fit does not record its rxode2 thread count, so \code{threads} must be supplied
by the caller (the count the bundle was fit at).
}
